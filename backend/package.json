{
  "name": "ratemypet-backend",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:staged": "vitest related --run --silent"
  }
}
