{
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@testcontainers/postgresql": "^11.6.0",
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^24.1.0",
    "@vitest/coverage-v8": "^3.2.4",
    "dbmate": "^2.28.0",
    "git-commit-msg-linter": "^5.0.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "prepare": "husky",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:staged": "vitest related --run --silent=true",
    "migrate": "dbmate --help",
    "migrate:new": "dbmate --env-file dbmate.env new"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "execa": "^9.6.0",
    "postgres": "^3.4.7",
    "zod": "^4.1.5"
  }
}
